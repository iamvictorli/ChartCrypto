{
  "name": "StockLi",
  "version": "1.0.0",
  "description": "Stock Market Charter with real time updates",
  "main": "server/index.js",
  "author": "Victor Li",
  "license": "MIT",
  "engines": {
    "node": "9.4.0",
    "npm": "5.6.0",
    "yarn": "1.3.2"
  },
  "scripts": {
    "build": "yarn && cd client && yarn && cd ..",
    "prod": "babel server -d dist --presets env --copy-files && cd client && yarn build && cd .. && node -r dotenv/config -r babel-register dist/index.js",
    "server": "nodemon --ignore client/ --exec node -r dotenv/config -r babel-register server/index.js",
    "client": "cd client && yarn start",
    "dev": "concurrently \"yarn run server\" \"yarn run client\"",
    "lint": "eslint server client/src",
    "heroku-postbuild": "babel server -d dist --presets env --copy-files && cd client && yarn --production=false && yarn run build"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-env": "^2.4.1",
    "babel-eslint": "^8.0.2",
    "concurrently": "^3.5.0",
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "socket.io": "^2.0.4"
  },
  "devDependencies": {
    "eslint": "4.9.0",
    "eslint-config-airbnb": "^16.1.0",
    "eslint-plugin-import": "2.7.0",
    "eslint-plugin-jsx-a11y": "6.0.2",
    "eslint-plugin-react": "7.4.0"
  }
}
